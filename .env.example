# RECOMMENDED: Run `claude-dockord setup` for full OAuth (enables Remote Control).
# Credentials persist in Docker volume across rebuilds.
#
# FALLBACK: Inference-only token (no Remote Control).
# Generate with `claude setup-token` on host.
# CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-paste-your-token-here
